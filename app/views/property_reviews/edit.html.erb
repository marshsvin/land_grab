<!-- Validation failure messages -->
<% if @property_review.errors.any? %>
  <% @property_review.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Property Review #<%= @property_review.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_property_review/<%= @property_review.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for property_review -->
      <div class="form-group">
        <label for="property_review" class="control-label">
          Property review
        </label>

        <input type="text" id="property_review" name="property_review" placeholder="Property review" class="form-control" value="<%= @property_review.property_review %>">
      </div>

      <!-- Label and input for property_id -->
      <div class="form-group">
        <label for="property_id" class="control-label">
          Property
        </label>

        <%= select_tag(:property_id, options_from_collection_for_select(Property.all, :id, :landowner_id, @property_review.property_id), :class => "form-control") %>
      </div>

      
      

      <button class="btn btn-warning">
        Update Property Review
      </button>
      or
      <a href="/property_reviews/<%= @property_review.id %>">Cancel</a>
    </form>
  </div>
</div>
